# framework-orders-plugin

A controlled example extension for the Telegram-First AI Developer Framework. It exports a `PluginManifest`, one action, and two tools using the public Extension SDK. Installation is intentionally explicit and controlled by the hosting environment; the API never installs or executes submitted source code.
